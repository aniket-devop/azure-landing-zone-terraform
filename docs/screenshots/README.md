# Screenshots to capture (do this before your next commit)

Placeholder folder — replace this file's contents once you've added the real images. Recruiters and interviewers weight "proof this ran" heavily; this folder is what closes that gap.

Capture and add these, named exactly as below, then reference them in the main README:

1. `terraform-plan-output.png` — sanitized terminal output of a real `terraform plan` against your sandbox subscription (blur/redact subscription IDs).
2. `azure-portal-resource-group.png` — the Azure Portal showing the deployed resource group with hub + spoke resources visible.
3. `github-actions-ci-pass.png` — a green run of `terraform-ci.yml` in the Actions tab.
4. `checkov-scan-result.png` — the Checkov summary output (pass/fail counts) from a CI run.
5. `network-topology-portal.png` — Azure Portal's "Network Topology" or "Diagram" view for the VNet, showing the actual peered hub/spoke.

Five screenshots, maybe 20 minutes of work, and it's the difference between "claims this ran" and "shows this ran."
